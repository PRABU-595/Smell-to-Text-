# Model Configuration
model:
  name: bert-base-uncased
  max_length: 128
  hidden_size: 768
  num_chemicals: 300
  dropout: 0.3
  freeze_encoder: false
  pooling: cls

# Alternative models
alternatives:
  - distilbert-base-uncased
  - roberta-base
  - microsoft/deberta-v3-base
